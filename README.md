# SMART_AGRICULTURE

Cách chạy Client:
    cd Client/my-farm-frontend
    npm init -y
    npm install
    npm run dev

Cách chạy Server:
    cd Server/farm-manager-api
    tải file .env sau đó dán vào folder cùng bậc với pom.xml
    mvn clean install
    ./mvnw spring-boot:run